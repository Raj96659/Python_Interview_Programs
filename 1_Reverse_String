Q. Write a python program to reverse a string.

def reverse_string(input_string):
    # Reverse the string using slicing
    return input_string[::-1]

# Input from the user
user_input = input("Enter a string to reverse: ")

# Call the function and display the reversed string
reversed_string = reverse_string(user_input)
print("Reversed string:", reversed_string)


# Without Inbuild Function

def reversed_string_without_indexing(s):
    reversed_str = ""
    for char in s:
        reversed_str = char + reversed_str
    return reversed_str

input_str = input("Enter the string :")

function_calling_without_indexing=reversed_string_without_indexing(input_str)
print("Reversed String without indexing : ",function_calling_without_indexing)
