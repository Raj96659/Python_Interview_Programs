def avg_sum(numbers):
    if not numbers:
        return none
    else:
        total = sum(numbers)
        average = total / len(numbers)
    return average

num_list = [1,3,5,6]
result = avg_sum(num_list)
print(result)
